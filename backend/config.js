//Credenciales de la base de datos
const config = {
    db: {
        host: 'localhost',
        user: 'root',
        password: '',
        database: 'gestion',
        connectTimeout: 10000,
    },
}

module.exports = config;